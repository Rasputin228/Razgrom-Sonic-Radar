# 🎯 Razgrom Sonic Radar

**Аудио-радар для Arena Breakout: Infinite и других шутеров.**
Программа анализирует звук из игры и визуализирует шаги врагов на экране вашего смартфона в стиле ASUS Sonic Radar Pro.

![Status](https://img.shields.io/badge/Status-Active-brightgreen) ![Python](https://img.shields.io/badge/Made%20with-Python-blue)

## ✨ Возможности
*   **Внешний дисплей:** Работает через браузер телефона (не перекрывает экран ПК).
*   **Безопасность:** Не внедряется в память игры (No Injection). Работает как анализ аудио-потока.
*   **Фильтрация своих шагов:** Умный алгоритм глушит звуки по центру и снижает чувствительность, когда вы нажимаете WASD.
*   **Sonic Pro Design:** 8 секторов, цветовая индикация угрозы, визуализация дистанции.
*   **Определение угрозы:** Анализ ритма звуков пытается отличить бота от реального игрока.

---

## 🚀 Скачать (Для игроков)
Вам не нужно устанавливать Python. Просто скачайте готовое приложение:

1.  Перейдите в раздел **[Releases](../../releases)** справа.
2.  Скачайте файл `RazgromRadar.exe`.
3.  Запустите от имени **Администратора** (важно для работы клавиатуры).
4.  В консоли появится IP-адрес (например, `192.168.1.35:5555`).
5.  Откройте этот адрес в браузере телефона.

---

## 🛠 Запуск из исходного кода (Для разработчиков)

Если вы хотите изменить код или запустить через Python:

1.  **Установите Python 3.10+** (Обязательно поставьте галочку "Add to PATH").
2.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/ВАШ_НИК/Razgrom-Sonic-Radar.git
    ```
3.  Установите библиотеки:
    ```bash
    pip install -r requirements.txt
    ```
4.  Запустите:
    ```bash
    python main.py
    ```

---
## 🧠 Техническая архитектура (How it works)

Этот проект написан на Python и работает как внешний DSP-процессор (Digital Signal Processing).

### Принцип работы:
1.  **Захват звука:** Используется библиотека `soundcard` и API Windows WASAPI Loopback. Программа слушает общий аудио-микс системы, не вторгаясь в процесс игры.
2.  **Фильтрация (DSP):**
    *   **Bandpass Filter:** Применяется фильтр Баттерворта (50Hz - 450Hz) для выделения низких частот (шаги) и отсечения высоких (ветер, сверчки).
    *   **Mid-Side Processing:** Используется матричная обработка для подавления центрального канала. Алгоритм вычитает левый канал из правого (`L - R`), удаляя звуки, звучащие по центру (собственные шаги игрока), и усиливая боковые звуки (враги).
3.  **Детекция движения:** Библиотека `keyboard` отслеживает нажатия WASD. При движении чувствительность радара динамически снижается, а подавление центра усиливается.
4.  **Сервер:** Данные передаются через `Flask-SocketIO` (WebSocket) на веб-клиент в реальном времени (60 FPS).
5.  **Клиент:** HTML5/JS интерфейс отрисовывает векторную графику (SVG), эмулируя работу аппаратных радаров.

---

## 📂 Структура проекта

*   `main.py` — **Основной код.** Содержит аудио-движок, веб-сервер и логику обработки.
*   `requirements.txt` — Список зависимостей.
*   `RazgromRadar.exe` (в Releases) — Скомпилированная версия через PyInstaller.

---

## ⚠️ Важно
*   **Античит:** Программа безопасна, так как не читает память игры, но используйте на свой страх и риск.
*   **Звук:** Для работы требуется, чтобы в системе работало устройство Loopback (обычно работает автоматически). Если не работает — включите "Стерео микшер" в Windows.
*   **Телефон:** Телефон и ПК должны быть подключены к одному Wi-Fi.

---
*Developed by RazgromProject*
